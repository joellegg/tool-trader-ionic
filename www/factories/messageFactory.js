factoryModule.factory('MessageFactory', function($http) {
  console.log("message factory initiated")

  // $http.get(`https://somewhat-pinterested.firebaseio.com/pins.json?orderBy="groupId"&equalTo="${groupId}"`)
});
